apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: heureka-service
  namespace: statex-apps
  labels:
    app: heureka-service
    managed-by: k8s-migration
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - heureka.alfares.cz
      secretName: heureka-service-tls
  rules:
    - host: heureka.alfares.cz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: heureka-service
                port:
                  number: 3800
