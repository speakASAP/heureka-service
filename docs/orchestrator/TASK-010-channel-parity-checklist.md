# TASK-010: Heureka Sales Channel Parity Checklist

Date: 2026-07-01
Repository: `/home/ssf/Documents/Github/heureka-service`
Status: active implementation checklist; parity slices deployed through `task-010-route-cleanup-20260701`; current Heureka category overrides, public descriptions, API gateway runtime, authenticated Catalog reads, guarded public mutations/reads, explicit dashboard shell subroutes, dashboard marketplace-category projection, read-only blocked-product lane verifier, read-only external feed preview, protected readiness-lanes dashboard handoff, dependency-aware health contract, gateway orders routing, optional gateway route fail-closed handling, redacted gateway error logging, and Heureka-local durable operation events are implemented through shared-style contracts

## Intent Preservation Chain

Vision -> Goal Impact -> System -> Feature -> Task -> Execution Plan -> Coding Prompt -> Code -> Validation

- Vision: Heureka should behave like the other Alfares sales-channel services and reuse their frontend, Auth, Catalog, Orders, Logging, health, deploy, and validation patterns.
- Goal Impact: reduce maintenance variance and make Heureka operationally comparable with Allegro, Bazos, Aukro, and FlipFlop.
- System: `heureka-service` on `alfares`, with Catalog, Orders, Auth, Warehouse, Logging, and Kubernetes runtime contracts.
- Feature: sales-channel parity across public UI, dashboard, product/feed lifecycle, integrations, observability, and deploy validation.
- Task: close ready gaps without overwriting the already merged TASK-009 landing/dashboard work.
- Execution Plan: this checklist extends `docs/orchestrator/TASK-010-channel-parity-gap-plan.md`.
- Coding Prompt: copy proven contract shapes from reference channels; do not invent a Heureka-only architecture.
- Code: see implementation status below.
- Validation: record exact command evidence after every changed slice.

## Current State Summary

Remote repo resolved as `/home/ssf/Documents/Github/heureka-service`; `/home/ssf/Documents/Github/heureka` does not exist.

Reference baseline:

- Allegro is the primary parity target for hosted Auth, landing, dashboard, protected dashboard routes, admin users, product workflow pages, API split, and deploy validation.
- Aukro is the closest feed-oriented dashboard reference for catalog candidate lists, current offers, bulk publish controls, dry-run readiness, and guarded mutation.
- Bazos is the static Nest-rendered fallback reference for compact hosted Auth and client/admin dashboard shell.
- FlipFlop is the storefront/Next.js reference and is not the primary Heureka UI model, but its hosted Auth and `/api` ingress split remain useful.

## Feature Matrix

| Feature | Heureka status | Reference parity | Gap | Implementation owner | Validation |
| --- | --- | --- | --- | --- | --- |
| Public landing `/` | implemented | Allegro/Bazos/Aukro/FlipFlop all expose a public entry | No code gap; verify live after deploy | Frontend/public | `curl -I https://heureka.alfares.cz/` |
| Hosted Auth `/login`, `/register` | implemented with registration caveat | Allegro/Bazos/FlipFlop redirect to hosted Auth without local credential forms | Live pages include hosted Auth URL and return/callback token handling; approved Auth client registration evidence remains external | Frontend/Auth | `/login` and `/register` include `auth.alfares.cz` |
| Auth callback `/auth/callback` | implemented | Hosted Auth standard and reference channels return tokens in the URL fragment, validate generated state, strip fragments, and store tokens | Heureka parses fragment tokens, strips the fragment before validation branches, validates state, stores tokens, and redirects safely; query `code/state` fallback is not part of hosted Auth | Frontend/Auth | route content and callback smoke |
| Protected dashboard shell | implemented | Allegro has nested `/dashboard/products`, orders, operations, admin users; Aukro has feed/bulk dashboard | Heureka now has server-rendered shells for products, feed, orders, operations, settings, and admin; live route split is validated | Frontend/dashboard | protected API returns `401` without token; shell returns `200` |
| Dashboard product list | implemented | Allegro/Aukro show catalog candidates with status, preview, guarded action, blocker-focused filters, and bulk candidate review | Heureka product list now returns read-only workflow projection, next action, blockers, filtered feed/workflow/gap views, uses Warehouse batch availability for page stock, and previews selected products through the read-only bulk readiness API | Dashboard API/UI | dashboard list self-test, service build, protected route smoke, bulk readiness smoke |
| Product detail/listing edit | implemented | Allegro/Aukro expose draft/detail before confirm | Heureka local listing edit writes Heureka `productName`/`categoryText` overrides to Catalog marketplace fields | Dashboard API | product detail includes `catalogMarketplaceProfile` |
| Dashboard orders | implemented | Allegro exposes protected order list/detail views backed by central Orders lifecycle | Heureka now exposes protected order list/detail APIs and a public dashboard shell route | Dashboard/orders | `/heureka/dashboard/orders` protected route smoke |
| Dashboard feed status/history/settings | implemented | Aukro exposes feed-oriented operational state and guarded settings/actions | Heureka now exposes feed status, feed history, settings read/update, and a `/dashboard/feed` shell route | Dashboard/feed | `/heureka/dashboard/feed/status` protected route smoke |
| Dashboard operations | implemented with durable local operation events plus derived timeline | Allegro/Bazos expose durable operation/audit evidence; Aukro exposes operational projections | Heureka now exposes operations summary/history, append-only `heureka_operation_events`, writer hooks for dashboard/feed/orders operations, and a derived fallback timeline; only ecosystem-wide adoption of the operation-event schema package remains `[MISSING]` | Dashboard/ops | `/dashboard/operations` shell, protected operation APIs, operation-event self-test, operations-history self-test, authenticated gateway smoke |
| Dashboard readiness lanes | implemented | Aukro/Allegro expose blocker-oriented product work queues and owner handoff states | Heureka now exposes protected read-only `GET /heureka/dashboard/readiness/lanes` and renders stock/media/catalog/settings blocker lanes on the operations dashboard without mutating Catalog, Warehouse, feed, media, or external Heureka | Dashboard/feed/data owners | dashboard self-test, service build, live authenticated readiness-lanes smoke |
| Catalog feed readiness | implemented | Catalog owns product truth; channel owns readiness/publication; Warehouse batch availability is the stock list contract | Heureka readiness/include/exclude exists and bulk readiness now uses Warehouse batch availability instead of N+1 stock reads | Feed/Catalog/Warehouse | `verify:heureka-stock-readiness-live`; repeated bulk readiness |
| Catalog Heureka marketplace connector | implemented | Catalog has content previews and marketplace fields for channels | Heureka dashboard now consumes protected Catalog content/profile routes and can update marketplace overrides | Dashboard API + Catalog client | service build and protected product detail smoke |
| Orders communication | implemented | Orders owns central order lifecycle | Heureka order ingestion contract matches `orders.create.v1`; runtime token source still needs confirmation | Orders integration | `npm run verify:heureka-order-ingestion` |
| Logging microservice | implemented for config parity plus local operation events | services send logs to `LOGGING_SERVICE_URL` and keep local fallback | Repo configmap includes live `LOGGING_SERVICE_URL`; Heureka now has an append-only local operation-event table/read model, while a shared cross-service audit schema remains a separate platform contract gap | Ops/logging | build, config diff review, live durable-event smoke |
| Health | implemented | all services expose `/health` and k8s probes; FlipFlop exposes dependency-aware health detail | `/health` remains lightweight for k8s probes; `/health/dependencies` now exposes read-only `heureka.dependency-health.v1` for DB/Auth/Catalog/Warehouse/Orders/Logging/Notification, and gateway dependency checks cover Heureka/Aukro/Auth/Logging | Ops/observability | `npm run verify:health-dependencies`, `curl -I https://heureka.alfares.cz/health`, `/health/dependencies` smoke |
| Kubernetes config | implemented | deploy manifests should match live runtime | repo configmap name/env aligned with live `heureka-config`; deployed in TASK-010 route-fix rollout | Ops/deploy | `npm run ips:deployment-readiness` |
| Docker runtime port | implemented | service listens on `3800` | Dockerfile now exposes `3800` | Build/deploy | service build |
| Gateway/nginx | implemented and deployed | reference channels route `/api/*` to API gateway and `/` to frontend/service | Heureka now has a live `heureka-api-gateway` deployment/service, `/api` ingress split, gateway `/api/health`, and known `/api/heureka/*` backend paths for feed, dashboard, products, orders, and health forwarding to Heureka while preserving unknown legacy Aukro fallback | Nginx/platform | gateway route self-test, api-gateway build, rollout, `/api/health`, protected unauthenticated smoke, and authenticated gateway forwarding smoke |
| External Heureka shop/import | unknown | channel-specific external onboarding is owner/data-dependent | `[UNKNOWN: shop approval and legal/company fields]`; category and description no longer block current active Catalog products, but image/stock quality still blocks some products | Owner + Catalog | external Heureka UI/API check |

## Implementation Order

1. Safe now: docs checklist, repo configmap parity, Docker `EXPOSE 3800`, production-safe JWT guard logging.
2. Safe now: backend Catalog content/profile consumption in Heureka dashboard detail.
3. Safe now: dashboard parity expansion for orders, feed status/history/settings, operations/history, and public shell routes.
4. Dependency-gated: deploy/restart and route smoke, because current live image is owned by the latest TASK-009 deployment until TASK-010 rollout.
5. Blocked: external Heureka registration/import completion until owner-supplied business/legal facts exist.

## Ready Implementation Tasks

| Task | Status | Allowed files | Forbidden files | Notes |
| --- | --- | --- | --- | --- |
| Repo runtime config parity | implemented | `k8s/configmap.yaml`, `Dockerfile` | service source files | Source manifests now match live config naming and service port expectations. |
| Auth guard logging hygiene | implemented | `shared/auth/jwt-auth.guard.ts` | public UI files | Production token-structure console logging is gated behind explicit debug mode. |
| Catalog profile dashboard integration | implemented | `shared/clients/catalog-client.service.ts`, `services/heureka-service/src/heureka/dashboard/dashboard.service.ts` | k8s, Dockerfile | Reads Catalog protected content/profile endpoints with service headers and writes Heureka marketplace overrides. |
| Dashboard UI parity expansion | implemented | `services/heureka-service/src/public/public.controller.ts`, `services/heureka-service/src/main.ts` | k8s | Product/detail, feed, orders, operations, settings, and admin shells are route-aware. |
| Dashboard orders API parity | implemented | `services/heureka-service/src/heureka/dashboard/dashboard.controller.ts`, `services/heureka-service/src/heureka/dashboard/dashboard.service.ts` | k8s, Dockerfile | Adds protected orders list/detail routes using existing Heureka order repository state. |
| Dashboard feed/settings API parity | implemented | `services/heureka-service/src/heureka/dashboard/dashboard.controller.ts`, `services/heureka-service/src/heureka/dashboard/dashboard.service.ts` | k8s, Dockerfile | Adds protected feed status/history/settings routes; settings update remains admin-gated. |
| Dashboard operations API parity | implemented | `services/heureka-service/src/heureka/dashboard/dashboard.controller.ts`, `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`, `services/heureka-service/src/heureka/operations/*`, `prisma/schema.prisma`, `prisma/migrations/202607011900_add_heureka_operation_events/migration.sql` | unrelated service contracts | Adds operations/history summary with durable Heureka-local operation events, append-only DB triggers, and derived timeline fallback; returns only `[MISSING: ecosystem-wide shared operation/audit schema package]` for the remaining platform-level gap. |
| Dashboard readiness lanes | implemented and deployed | `services/heureka-service/src/heureka/dashboard/dashboard.controller.ts`, `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`, `services/heureka-service/src/public/public.controller.ts`, dashboard self-test | Warehouse/Catalog/media/feed/external Heureka mutation endpoints | Adds protected read-only owner handoff lanes for stock, media, Catalog content, and feed settings using existing Catalog search and Heureka bulk readiness contracts; operations dashboard now shows lane status and blocked-product next actions. |
| Orders/logging runtime smoke | implemented | scripts/reports only | source code unless smoke exposes a defect | Protected dashboard API routes and live health/readiness smokes passed after route-fix deployment. |
| Heureka bulk stock readiness stability | implemented | `shared/clients/warehouse-client.service.ts`, `services/heureka-service/src/heureka/feed/feed.service.ts`, readiness self-test | Warehouse mutation endpoints | Uses Warehouse `POST /api/stock/availability/batch` for product lists; missing rows remain `ZERO_STOCK`, dependency gaps become `STOCK_UNKNOWN` instead of false zero-stock. |
| Dashboard product workflow projection | implemented | `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`, `services/heureka-service/src/public/public.controller.ts`, dashboard self-test | Catalog/Warehouse mutation endpoints | Adds workflow status, next action, blockers, read-only filters, selected-product bulk readiness preview, Catalog preview visibility in detail, and early hosted Auth fragment stripping. |
| API gateway Heureka route parity | implemented and deployed | `services/api-gateway/src/gateway/gateway.controller.ts`, `services/api-gateway/src/gateway/gateway.service.ts`, `services/api-gateway/Dockerfile`, `services/api-gateway/package.json`, `k8s/api-gateway-deployment.yaml`, `k8s/api-gateway-service.yaml`, `k8s/ingress.yaml`, `scripts/deploy.sh`, gateway self-test | unrelated service contracts | Known Heureka-owned `/api/heureka/feed`, `/api/heureka/dashboard`, `/api/heureka/products`, `/api/heureka/orders`, and `/api/heureka/health` paths resolve to `heureka-service`; unknown legacy paths keep Aukro compatibility; `/api` ingress now targets live `heureka-api-gateway`. |
| Operations durable event log | implemented | `prisma/schema.prisma`, `prisma/migrations/202607011900_add_heureka_operation_events/migration.sql`, `services/heureka-service/src/heureka/operations/*`, dashboard/feed/orders services, operations self-tests | shared platform logging schema | Adds append-only Heureka `heureka_operation_events`, redacted durable read model, and writer hooks for dashboard listing/settings/regenerate, feed generation/include/exclude, and order ingestion/forwarding. |
| Operations derived timeline | implemented | `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`, `services/heureka-service/src/public/public.controller.ts`, operations self-test | shared platform logging schema | Keeps the read-only `operationEvents` fallback timeline from existing Heureka feed/order/offer/product/settings rows and overlays durable operation events when present. |
| Catalog monitor Service selector fix | implemented in Catalog repo | `/home/ssf/Documents/Github/catalog-microservice/k8s/contract-monitor-cronjob.yaml` | Catalog API deployment/service selector | Monitor pods no longer carry `app=catalog-microservice`, so Catalog Service endpoints route only to the API pod. |
| Dependency-aware health parity | implemented | `shared/health/health.service.ts`, `shared/health/health.service.self-test.ts`, `services/heureka-service/src/health/health.controller.ts`, `services/api-gateway/src/health/health.controller.ts` | k8s probes, service mutation endpoints | Keeps `/health` unchanged for probes and adds read-only `/health/dependencies` for Heureka service and API gateway dependency evidence. |

## Implementation Evidence

- `Dockerfile`: changed exposed runtime port from `3000` to `3800`.
- `k8s/configmap.yaml`: aligned source config name/env keys with live `heureka-config`, including `LOGGING_SERVICE_URL`, Auth/Warehouse/Notification/RabbitMQ URLs, gateway timeout keys, and `DB_HOST=db-server-postgres`.
- `shared/auth/jwt-auth.guard.ts`: removed production token-structure console logging; detailed guard logs now require `NODE_ENV=development` or `AUTH_GUARD_DEBUG=true`.
- `shared/clients/catalog-client.service.ts`: added protected Catalog read/write helpers for Heureka content preview and marketplace fields using internal service headers; TASK-010 follow-up now sends the same internal service headers on protected product reads, SKU reads, search, pricing, media, feed snapshot, create, and update calls, with product/SKU/feed identifiers URL-encoded.
- `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`: product detail now includes Catalog content/profile data, uses Heureka `categoryText` in listing/gap calculation, and saves dashboard marketplace overrides to Catalog.
- `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`: added dashboard parity APIs for orders list/detail, feed status/history/settings, operations summary/history, runtime status, and operation action metadata.
- `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`: product list now uses Warehouse batch availability, returns workflow status, next action, blockers, and read-only filters for feed/workflow/data-gap state.
- `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`: added protected read-only readiness lanes that page active Catalog products, call existing bulk feed readiness in 100-product chunks, group blocker codes by owner lane, and return machine-actionable handoff actions.
- `services/heureka-service/src/heureka/dashboard/dashboard.controller.ts`: added `GET /heureka/dashboard/readiness/lanes` behind the existing dashboard JWT guard.
- `services/heureka-service/src/public/public.controller.ts`: product dashboard filters now drive the protected catalog-products API, table/detail panel display workflow blockers and next actions, Catalog Heureka preview/profile status is visible in the listing panel, and hosted Auth callback strips URL fragments before all validation branches.
- `services/heureka-service/src/public/public.controller.ts`: operations dashboard now renders readiness lane status and the top blocked-product handoff rows from the protected readiness lanes endpoint.
- `services/heureka-service/src/heureka/dashboard/dashboard-list-products.self-test.ts`: locks dashboard list behavior to Warehouse batch stock and workflow projection filtering.
- `services/heureka-service/src/heureka/dashboard/dashboard-list-products.self-test.ts`: locks protected readiness-lanes aggregation, read-only/no-mutation metadata, and controller `feedType` forwarding.
- `services/api-gateway/src/gateway/gateway.service.ts`: added `isHeurekaServiceBackendPath` route classification for Heureka-owned feed, dashboard, product, order, and health APIs.
- `services/api-gateway/src/gateway/gateway.controller.ts`: `/api/heureka/*` now sends known Heureka-owned paths to `heureka-service` and keeps unknown legacy paths on the existing Aukro compatibility fallback.
- `services/api-gateway/src/gateway/gateway-route-parity.self-test.ts`: locks the source-level route split for Heureka-owned and legacy fallback paths.
- `services/api-gateway/Dockerfile`: exposes/listens on `3801`, probes `/api/health`, and installs OpenSSL in builder/runtime stages so Prisma generates a compatible client for the deployed Node image.
- `services/api-gateway/package.json` and `package-lock.json`: add `class-validator` and `class-transformer`, matching the Nest `ValidationPipe` dependency used by the reference gateways.
- `k8s/api-gateway-deployment.yaml` and `k8s/api-gateway-service.yaml`: add the live `heureka-api-gateway` workload on port `3801` with Auth, Heureka, Aukro, Logging, and DB runtime config.
- `k8s/ingress.yaml`: routes `/api` to `heureka-api-gateway:3801`, keeps `/health` and `/` on `heureka-service:3800`.
- `scripts/deploy.sh`: builds, pushes, applies, annotates, and waits for both `heureka-service` and `heureka-api-gateway`; gateway preflight warns instead of blocking recovery from a failed gateway rollout.
- `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`: operations history now overlays durable `heureka_operation_events` with derived events from existing `heureka_feeds`, `heureka_orders`, `heureka_offers`, `heureka_products`, and `heureka_settings` rows, preserving explicit durability metadata.
- `services/heureka-service/src/public/public.controller.ts`: operations dashboard now renders durable Heureka operation events and derived history alongside feed/runtime history.
- `services/heureka-service/src/heureka/dashboard/dashboard-operations-history.self-test.ts`: locks durable-event overlay behavior, read-only safety metadata, and the remaining shared-schema blocker.
- `prisma/schema.prisma`: added `HeurekaOperationEvent` mapped to `heureka_operation_events` with snake_case column mappings and query indexes for action, status, feed type, account, product, entity, external id, correlation id, and creation time.
- `prisma/migrations/202607011900_add_heureka_operation_events/migration.sql`: creates the append-only `heureka_operation_events` table, partial unique `idempotency_key` index, supporting indexes, and update/delete prevention triggers.
- `services/heureka-service/src/heureka/operations/operation-event.service.ts`: adds redacted append/list service with `durability=durable_operation_event`, read-only safety metadata, duplicate idempotency handling, and missing-table fallback.
- `services/heureka-service/src/heureka/operations/operation-event.service.self-test.ts`: locks redaction, durable serialization, and raw-payload safety behavior.
- `services/heureka-service/src/heureka/dashboard/dashboard.module.ts`, `feed.module.ts`, and `orders.module.ts`: import the operation-event module so dashboard, feed, and order flows share the same writer.
- `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`, `feed.service.ts`, and `orders.service.ts`: write durable operation events for listing changes, settings updates, feed regeneration, feed inclusion/exclusion, order receipt, forwarding, and replay without storing raw customer payloads.
- `services/heureka-service/src/heureka/dashboard/dashboard.controller.ts`: added protected routes for `/heureka/dashboard/orders`, `/heureka/dashboard/feed/*`, `/heureka/dashboard/operations`, and `/heureka/dashboard/settings`.
- `services/heureka-service/src/main.ts`: keeps conflicting protected API routes under `/heureka/dashboard/*` and excludes exact public shell paths `/dashboard/products`, `/dashboard/feed`, `/dashboard/orders`, `/dashboard/operations`, `/dashboard/settings`, and `/dashboard/admin/users` from the global `/heureka` prefix.
- `services/heureka-service/src/public/public.controller.ts`: exposes explicit dashboard shell routes for products, feed, orders, operations, settings, and admin users instead of middleware URL rewriting.
- `services/heureka-service/src/public/public.controller.ts`: dashboard listing form now exposes editable `Heureka category`; shell navigation now includes Feed, Orders, Operations, Settings, Admin Users and loads the matching protected APIs per route.
- Catalog marketplace profiles: populated 32 Heureka `overrides.categoryText` values through protected `PUT /api/products/:productId/marketplace-fields/heureka`, matching the shared Catalog-owned marketplace override pattern used by reference channels. No Heureka-local category fallback was added.
- Catalog canonical product content: populated 23 missing public `description` / `descriptionRich` values through protected `PUT /api/products/:id`, matching the shared Catalog-owned description pattern confirmed by Allegro, Bazos, Aukro, and FlipFlop. No Heureka-local description fallback was added.
- `shared/clients/warehouse-client.service.ts`: added Warehouse batch availability read client for the documented `POST /api/stock/availability/batch` contract.
- `services/heureka-service/src/heureka/feed/feed.service.ts`: bulk and single readiness now resolve stock through the Warehouse batch availability contract, avoiding unstable N+1 stock calls and preserving `STOCK_UNKNOWN` when batch evidence is missing.
- `/home/ssf/Documents/Github/catalog-microservice/k8s/contract-monitor-cronjob.yaml`: changed monitor pod labels from `app=catalog-microservice` to `app=catalog-contract-monitor` so scheduled monitor pods cannot enter the Catalog API Service endpoints.
- `services/heureka-service/src/heureka/operations/operation-event.schema.ts`: defines local `heureka.operation.audit.v1`, redacted audit envelope projection, and generic logging DTO projection for durable Heureka operation rows without emitting new runtime logs.
- `services/heureka-service/src/heureka/operations/operation-event.service.ts`: serializes durable operation rows with `schemaRef`, `auditEnvelope`, `loggingEvent`, and schema-aware read-model safety metadata.
- `services/heureka-service/src/heureka/dashboard/dashboard.service.ts`: operation history now exposes the schema-aware read model and narrows the remaining missing marker to `[MISSING: ecosystem-wide shared operation/audit schema package]`.
- `16_operations/INTEGRATIONS.md` and `23_documentation_contracts/TASK-007_EVENT_TAXONOMY.md`: document the non-emitting Heureka operation audit adapter, envelope fields, logging DTO projection, and ecosystem-level schema-package blocker.
- `.gitignore`: explicitly unignores `prisma/migrations/**` so `prisma/migrations/202607011900_add_heureka_operation_events/migration.sql` remains reviewable instead of hidden by the broad migration ignore rule.
- `shared/clients/catalog-client.service.self-test.ts`: locks internal service headers for protected Catalog reads/search/snapshot/write calls and verifies URL encoding for product IDs, SKUs, and feed snapshot IDs.
- `services/heureka-service/src/heureka/orders/orders.controller.ts` and `orders.module.ts`: guard public order read endpoints with hosted Auth JWT parity, keeping external ingestion on the existing service-token guard.
- `services/heureka-service/src/heureka/feed/feed.controller.ts`: guard public feed regeneration with `HeurekaFeedMutationGuard` so unauthenticated calls cannot mutate feed lifecycle state.
- `services/heureka-service/src/heureka/dashboard/dashboard.controller.ts`: forward `feedStatus`, `workflowStatus`, and `gap` filters to dashboard product workflow projection.
- `services/api-gateway/src/gateway/gateway.controller.ts` and `gateway.service.ts`: remove raw auth/body/response preview logging and preserve upstream non-redirect HTTP status codes through the gateway response wrapper.
- `services/heureka-service/src/heureka/dashboard/dashboard-list-products.self-test.ts` and `services/api-gateway/src/gateway/gateway-route-parity.self-test.ts`: lock dashboard filter passthrough and gateway status propagation.
- `services/api-gateway/src/gateway/gateway.service.ts` and `services/api-gateway/src/gateway/gateway-route-parity.self-test.ts`: route Heureka-owned `/api/heureka/orders*` paths to `heureka-service` instead of the legacy Aukro fallback.
- `shared/health/health.service.ts`, `services/heureka-service/src/health/health.controller.ts`, and `services/api-gateway/src/health/health.controller.ts`: keep `/health` as a lightweight probe and add dependency-aware `/health/dependencies` with `heureka.dependency-health.v1`, `readOnly=true`, and `mutations=[]`.
- `shared/health/health.service.self-test.ts`: locks basic health backward compatibility, dependency URL normalization, logging `/api/logs` to `/health` mapping, and critical DB failure classification.

## Open Blockers

- `[UNKNOWN: dedicated production secret source for HEUREKA_INTERNAL_SERVICE_TOKEN]`; live currently sources `HEUREKA_INTERNAL_SERVICE_TOKEN` from `catalog-microservice-secret/CATALOG_INTERNAL_SERVICE_TOKEN` so Catalog can call Heureka feed mutation routes.
- `[UNKNOWN: whether external Heureka shop approval must complete before feed validity details update]`.
- `[RESOLVED: Heureka category mapping for current Catalog products]`; current active Catalog products no longer report `MISSING_CATEGORY` after Heureka `categoryText` overrides were populated in Catalog marketplace profiles.
- `[RESOLVED: product public descriptions for current Catalog products]`; current active Catalog products no longer report `MISSING_DESCRIPTION` after canonical descriptions were populated in Catalog product content.
- `[RESOLVED: Catalog monitor pods entering Catalog API Service endpoints]`; live `catalog-microservice` endpoints now contain only the Catalog API pod after the CronJob label fix and active broken monitor job cleanup.
- `[RESOLVED: Heureka bulk readiness unstable stock counts]`; live Heureka now uses Warehouse batch availability and repeated bulk readiness is stable.
- `[MISSING: stock for 25 current active products]`; latest in-pod blocked-lane verifier on `task-010-route-cleanup-20260701` reports `ZERO_STOCK` for 25 active products and no missing Warehouse rows.
- `[MISSING: primary image for 12 current active products]`; latest in-pod blocked-lane verifier reports 12 missing primary images and no approved image URLs/files for those products.
- `[MISSING: public Heureka category text and public VAT-inclusive price for 1 current active product]`; latest in-pod blocked-lane verifier reports `8edc51f2-bed2-433f-8a3c-5738b49a02e1` (`EAN4893575894` / `Cvetki gorshki`) missing category and price.
- `[MISSING: authoritative current stock source for 23 order-history-only Allegro products]`; order history is not stock authority, current Allegro stock snapshots are absent for these products, and live Allegro current-stock API returned no rows because all configured OAuth tokens reported `expired_not_refreshed` during the read-only audit.
- `[MISSING: owner decision and quantity source for PROD9998]`; Warehouse currently reports `totalAvailable=0`.
- `[MISSING: approved public image URLs or image files for the 11 affected zero-stock products]`; Catalog media read reports `mediaCount=0` for those products.
- `[RESOLVED: persisted Allegro/Catalog source evidence for the 11 missing-image products]`; guarded Allegro offer API plus Catalog product/media reads show all 11 are `ORDER_HISTORY`/`PARTIAL`, include `images` in `missingFields`, have `imagesCount=0`, `rawImageCount=0`, and no Catalog media URLs.
- `[MISSING: live Allegro current-stock read for affected offer IDs]`; persisted order-history rows are not current stock authority, so OAuth refresh/read remains gated before using Allegro as a stock source.
- `[UNKNOWN: which zero-stock products should be listed on Heureka now]`; current behavior keeps them excluded from public feed output.
- `[MISSING: owner-supplied e-shop registration legal/company fields for external Heureka completion]`.
- `[RESOLVED: Heureka-local durable operation/audit log contract]`; Heureka now has append-only `heureka_operation_events`, DB update/delete guards, redacted read model, and dashboard/feed/orders writer hooks.
- `[RESOLVED: Heureka-local operation/audit schema adapter]`; `heureka.operation.audit.v1` now maps durable rows to a redacted audit envelope and logging DTO projection without emitting new logs.
- `[RESOLVED: Heureka authenticated Catalog read path for readiness]`; protected Catalog product reads/search/snapshot/pricing/media calls now send internal service headers and URL-encode identifiers, restoring live bulk readiness from `PRODUCT_NOT_FOUND=24` to `ZERO_STOCK=24` for the affected products.
- `[MISSING: ecosystem-wide shared operation/audit schema package]`; Heureka-local schema conformance is implemented, but cross-service package adoption across Allegro/Bazos/Aukro/logging remains undefined.
- `[RESOLVED: public Heureka order read endpoint guard parity]`; unauthenticated `GET /heureka/orders` and `GET /heureka/orders/:id` are now protected by `JwtAuthGuard`, matching dashboard order access.
- `[RESOLVED: public feed regeneration mutation guard parity]`; `POST /heureka/feed/regenerate` now requires `HeurekaFeedMutationGuard`.
- `[RESOLVED: dashboard product filter passthrough]`; `feedStatus`, `workflowStatus`, and `gap` query params now reach `DashboardService.listProducts`.
- `[RESOLVED: API gateway raw payload logging/status passthrough parity]`; gateway logging no longer records raw auth/body previews and non-redirect upstream statuses are preserved in responses.
- `[RESOLVED: dashboard list marketplace category projection]`; dashboard product list now reads Catalog Heureka marketplace fields and uses `categoryText` overrides for visible category and gap calculation, matching product detail/readiness behavior.
- `[RESOLVED: TASK-010 production rollout]`; initial rollout was deferred during unrelated FlipFlop/Leads activity, then deployed successfully as `localhost:5000/heureka-service:task-010-channel-parity-route-fix-20260701`.
- `[UNKNOWN: owner of k8s/external-secret.yaml runtime change]`; during TASK-010 validation, a parallel process changed `JWT_SECRET` remoteRef from `secret/prod/heureka-service` to `secret/prod/auth-microservice`, applied the ExternalSecret, and restarted Heureka on the previous TASK-009 image. The new pod became healthy; this was not a TASK-010 image deployment.
- `[RESOLVED: deployed Heureka api-gateway runtime]`; `statex-apps` now has `heureka-api-gateway` deployment/service and ingress routes `/api` to the gateway.
- `[RESOLVED: Heureka operation-event runtime rollout]`; `statex-apps` now runs `localhost:5000/heureka-service:task-010-operation-events-20260701b` and `localhost:5000/heureka-api-gateway:task-010-operation-events-20260701b`.

## Validation Bundle

Run after code edits:

```bash
git diff --check
npm --prefix shared run build
LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build
npm run verify:health-dependencies
NPM_CONFIG_CACHE=/tmp/heureka-npm-cache npm run verify:task-010-source-parity
npm run verify:heureka-external-readiness
npm run verify:heureka-order-ingestion
npm run verify:heureka-stock-readiness-live
```

Pre-deploy validation evidence captured on 2026-07-01:

- `git diff --check` -> PASS.
- `npm run ips:audit` -> PASS, documentation audit score 100/100.
- `npm run ips:pre-coding` -> PASS, report `reports/validation/ips-pre-coding-gate.json`.
- `npm run ips:deployment-readiness` -> PASS, report `reports/validation/ips-deployment-readiness-gate.json`.
- `npm --prefix shared run build` -> PASS.
- `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build` -> PASS.
- `npm run verify:heureka-order-ingestion` -> PASS.
- In-pod `npm run verify:heureka-stock-readiness-live` from `heureka-service-64dd4994fd-w6qhd` -> PASS for stock contract; product `884c1c5e-fe94-46c7-aab1-78bcc424e7ee` has Warehouse/readiness stock `60/60`, readiness `blocked`, blocker `MISSING_CATEGORY`.
- Production route smoke on current TASK-009 image -> `/`, `/health`, `/dashboard` return `200`; `/heureka/dashboard/me` returns expected unauthenticated `401`.
- After parallel ExternalSecret restart, Heureka remained on image `localhost:5000/heureka-service:task-009-dashboard-visual-fidelity-20260701d`; new pod returned `/health` 200 and `/heureka/dashboard/me` 401 without token.
- First TASK-010 deploy by a parallel process produced image `localhost:5000/heureka-service:task-010-admin-users-20260701a`; live smoke exposed a route bug where `/heureka/dashboard/orders` and `/heureka/dashboard/settings` returned `404` because exact public dashboard exclusions stripped the API prefix.
- Route-fix build `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build` -> PASS.
- Route-fix deploy `./scripts/deploy.sh task-010-channel-parity-route-fix-20260701` -> PASS; image digest `sha256:5cdd2aa32a5c397849dc694b5700c539d1738697361c23594d7272908e637145`; deployment image `localhost:5000/heureka-service:task-010-channel-parity-route-fix-20260701`; pod `heureka-service-bb56946f4-ghq9q` `1/1 Running`.
- Post-deploy public route smoke -> `/`, `/health`, `/dashboard`, `/dashboard/feed`, `/dashboard/orders`, `/dashboard/operations`, `/dashboard/settings`, `/dashboard/admin/users` all returned `200`.
- Post-deploy hosted Auth smoke -> `/login`, `/register`, and `/auth/callback` returned `200` and included `auth.alfares.cz`, `return_to`, and access-token callback markers.
- Post-deploy protected route smoke without token -> `/heureka/dashboard/me`, `/heureka/dashboard/orders`, `/heureka/dashboard/orders/test`, `/heureka/dashboard/feed/status`, `/heureka/dashboard/feed/history`, `/heureka/dashboard/feed/settings`, `/heureka/dashboard/operations`, `/heureka/dashboard/operations/history`, `/heureka/dashboard/settings` all returned `401`.
- Post-deploy Nest route log confirms protected mappings for `/heureka/dashboard/orders`, `/heureka/dashboard/orders/:id`, `/heureka/dashboard/operations`, `/heureka/dashboard/operations/history`, and `/heureka/dashboard/settings`.
- Post-deploy in-pod `npm run verify:heureka-stock-readiness-live` from `heureka-service-bb56946f4-ghq9q` -> PASS for stock contract; product `884c1c5e-fe94-46c7-aab1-78bcc424e7ee` has Warehouse/readiness stock `60/60`, readiness `blocked`, blocker `MISSING_CATEGORY`.
- Category population dry-run through Catalog marketplace-fields contract -> 32 proposed, 8 skipped with no matching rule; proposed groups: 13 memory cards, 4 USB flash drives, 14 snowtubing sleds, 1 party balloon.
- Category population apply through Catalog marketplace-fields contract -> 32 Heureka `overrides.categoryText` values written; post-apply dry-run -> `existing_categoryText=32`, `no_rule=8`, `proposedCount=0`.
- Post-category live default verifier `npm run verify:heureka-stock-readiness-live` from `heureka-service-bb56946f4-ghq9q` -> PASS; product `884c1c5e-fe94-46c7-aab1-78bcc424e7ee` has Warehouse/readiness stock `60/60`, readiness `ready`, blockerCodes `[]`.
- Post-category bulk readiness for 40 active products -> `ready=16`, `blocked=24`, `missingCategoryCount=0`; remaining blocker counts: `MISSING_DESCRIPTION=23`, `ZERO_STOCK=24`, `MISSING_PRIMARY_IMAGE=11`.
- Post-category Catalog snapshot for `884c1c5e-fe94-46c7-aab1-78bcc424e7ee` -> `CATEGORYTEXT="Sport | Zimní sporty | Sáně a boby"`, `overridesApplied=["categoryText"]`.
- Post-category public feed smoke -> `https://heureka.alfares.cz/heureka/feed?type=heureka_cz` returned `200` and rendered `CATEGORYTEXT` tags.
- Description contract inspection subagent -> PASS; confirmed Heureka `DESCRIPTION` must come from Catalog canonical `Product.descriptionRich` / `Product.description`, while `product_marketplace_profiles.overrides` remains for small channel-specific values.
- Description population dry-run through Catalog product update contract -> 23 proposed, 17 skipped with existing descriptions; proposed groups: 13 memory cards, 4 USB flash drives, 6 snowtubing sleds.
- Description population apply through Catalog `PUT /api/products/:id` -> command completed with code 0; post-apply dry-run -> `proposedCount=0`, `skippedByReason.description_not_missing=40`.
- Post-description bulk readiness for 40 active products -> `ready=16`, `blocked=24`; `MISSING_DESCRIPTION` and `MISSING_CATEGORY` absent; remaining blocker counts: `ZERO_STOCK=24`, `MISSING_PRIMARY_IMAGE=11`.
- Post-description Catalog Heureka snapshot for `e0034f63-53be-4287-954e-5d519eb57a79` -> `contractVersion="catalog-heureka-feed-snapshot.v1"`, `hasDescription=true`, `CATEGORYTEXT="Elektronika | Počítače a kancelář | Paměťové karty"`.
- Post-description in-pod feed smoke `http://127.0.0.1:3800/heureka/feed?type=heureka_cz` -> `200`, `X-Heureka-Feed-Status=valid`, `SHOPITEM=7`, `DESCRIPTION=7`.
- Catalog Service selector incident -> reproduced intermittent `ECONNREFUSED` from Heureka to `catalog-microservice:3200`; live endpoints showed `catalog-contract-monitor-*` job pod selected by `catalog-microservice` Service.
- Catalog monitor label fix -> applied `/home/ssf/Documents/Github/catalog-microservice/k8s/contract-monitor-cronjob.yaml`, deleted active broken monitor job `catalog-contract-monitor-29715420`, and verified `catalog-microservice` endpoint is only `10.42.0.61:3200`.
- Heureka batch-readiness focused self-test -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options '{"types":["node"]}' services/heureka-service/src/heureka/feed/feed-readiness-settings.self-test.ts`.
- Heureka batch-readiness source builds -> PASS: `npm --prefix shared run build`; PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; PASS: `git diff --check`.
- Heureka deploy `./scripts/deploy.sh task-010-batch-readiness-20260701` -> image built/pushed with digest `sha256:4d6df126f00664ac32314bece63b81458427587bd1227c1f88dc34aecb61299b`; rollout script timed out during image pull, then deleting stuck pod allowed rollout to complete.
- Live Heureka image -> `localhost:5000/heureka-service:task-010-batch-readiness-20260701`; pod `heureka-service-55968cfff8-zhrhh` `1/1 Running`.
- Post-batch readiness repeated in-pod validation from the live pod -> three consecutive runs all returned `productCount=40`, `readyCount=16`, `blockedCount=24`, blocker counts `ZERO_STOCK=24`, `MISSING_PRIMARY_IMAGE=11`.
- Post-batch feed smoke from live pod -> `http://127.0.0.1:3800/heureka/feed?type=heureka_cz` returned `200`, `X-Heureka-Feed-Status=valid`, `SHOPITEM=7`, `DESCRIPTION=7`, `CATEGORYTEXT=7`.
- Post-batch public route smoke -> live image tag confirmed; `/health` returned `200`, `/dashboard` returned `200`, protected `/heureka/dashboard/me` returned expected unauthenticated `401`.
- Dashboard product-list batch/workflow self-test -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options '{"types":["node"]}' services/heureka-service/src/heureka/dashboard/dashboard-list-products.self-test.ts`.
- Dashboard workflow build validation -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`.
- Dashboard workflow deploy `./scripts/deploy.sh task-010-dashboard-workflow-20260701` -> PASS; image digest `sha256:983581dbe44bcc901592df30a20fb4559cd4d1cc06f80209b2b51bf9b172f427`; active image `localhost:5000/heureka-service:task-010-dashboard-workflow-20260701`; pod `heureka-service-859fc757f9-cvdt2` `1/1 Running`.
- Post-dashboard-workflow public smoke -> `/health`, `/dashboard`, and `/auth/callback` returned `200`; served callback HTML contains early fragment stripping; protected `/heureka/dashboard/me` returned expected unauthenticated `401`.
- Post-dashboard-workflow authenticated in-pod smoke -> `GET /heureka/dashboard/catalog-products?limit=10&workflowStatus=blocked` returned `200`, `returned=10`, sample `workflowStatus=blocked`, sample `nextAction=resolve_data_gaps`, sample blocker count `2`.
- Bulk selected-product preview UI build validation -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; the UI calls existing read-only `POST /heureka/feed/readiness/bulk` for selected product IDs.
- Bulk preview deploy `./scripts/deploy.sh task-010-bulk-preview-20260701` -> PASS; image digest `sha256:8e6b115ed76b6fe10c38a1ac1afb36897c62a33fd12a0f15105bd6c710feb39b`; active image `localhost:5000/heureka-service:task-010-bulk-preview-20260701`; pod `heureka-service-79584fbd86-b54rr` `1/1 Running`.
- Post-bulk-preview public smoke -> `/health`, `/dashboard`, and `/auth/callback` returned `200`; served callback HTML contains early fragment stripping; protected `/heureka/dashboard/me` returned expected unauthenticated `401`.
- Post-bulk-preview authenticated in-pod smoke -> `GET /heureka/dashboard/catalog-products?limit=10&workflowStatus=blocked` returned `200`, `returned=10`, sample `workflowStatus=blocked`, sample `nextAction=resolve_data_gaps`; selected-product `POST /heureka/feed/readiness/bulk` returned `201`, `total=3`, `blocked=3`.
- API gateway Heureka route parity self-test -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options '{"types":["node"]}' services/api-gateway/src/gateway/gateway-route-parity.self-test.ts`.
- API gateway source build -> PASS with `npm --prefix services/api-gateway run build`.
- API gateway runtime inventory -> `statex-apps` has `heureka-service` deployment/service/ingress but no deployed Heureka API gateway deployment/service; production traffic still uses direct Heureka ingress.
- API gateway runtime implementation validation -> PASS: `bash -n scripts/deploy.sh`; PASS: `npm --prefix services/api-gateway run build`; PASS: gateway route parity self-test; PASS: `git diff --check`.
- API gateway first runtime rollout `./scripts/deploy.sh task-010-api-gateway-runtime-20260701` exposed missing gateway runtime dependencies (`class-validator`/`class-transformer`) and then Prisma OpenSSL engine mismatch; fixed in gateway package and Dockerfile before final rollout.
- API gateway runtime deploy `./scripts/deploy.sh task-010-api-gateway-runtime-20260701c` -> PASS; service image digest `sha256:621c394567fff6e6fc3b8f35be5f8aa4a9482d16d317624bbde373fb449502c1`; gateway image digest `sha256:17eadf0d1275c6bcaf37e14c309d3d662273418e40a2c6b07e5ec423b643c9ed`; pods `heureka-service-8478966c88-hpxwc` and `heureka-api-gateway-658d776bc9-c6kn9` `1/1 Running`.
- Post-gateway-runtime ingress inventory -> `PATH=/api SVC=heureka-api-gateway PORT=3801`, `PATH=/health SVC=heureka-service PORT=3800`, `PATH=/ SVC=heureka-service PORT=3800`.
- Post-gateway-runtime public smoke -> `/api/health` returned `200` with `service=heureka-api-gateway`; `/health` returned `200` with `service=heureka-service`; `/dashboard/operations` returned `200`; `/api/heureka/dashboard/summary` without token returned expected `401`.
- Post-gateway-runtime authenticated in-pod gateway smoke -> `GET /api/heureka/dashboard/operations/history` through `heureka-api-gateway` returned `200`, `success=true`, `operationEvents=34`, and durable-audit `[MISSING: ...]` markers remained explicit.
- Operation-event migration applied live via postgres pod -> `to_regclass(public.heureka_operation_events)=heureka_operation_events`, trigger count `2`.
- Operation-event schema fix -> mapped Prisma model fields to existing snake_case migration columns after live smoke found `P2022 column feedType does not exist`; `./shared/node_modules/.bin/prisma validate --schema=prisma/schema.prisma` -> PASS.
- Operation-event service self-test -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options {types:[node]} services/heureka-service/src/heureka/operations/operation-event.service.self-test.ts`.
- Operation-event dashboard-history self-test -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options {types:[node]} services/heureka-service/src/heureka/dashboard/dashboard-operations-history.self-test.ts`.
- Operation-event build validation -> PASS: `npm --prefix shared run build`; PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; PASS: `npm --prefix services/api-gateway run build`; PASS: `git diff --check`.
- Operation-event IPS/order validation -> PASS: `npm run ips:audit`; PASS: `npm run ips:pre-coding`; PASS: `npm run ips:deployment-readiness`; PASS: `npm run verify:heureka-order-ingestion`.
- Operation-event deploy `./scripts/deploy.sh task-010-operation-events-20260701b` -> images pushed with service digest `sha256:2ac522b761625c5357a6c29d52e1518819053b862f8101f09a5e3a7b52b89cf0` and gateway digest `sha256:fce1298c8384834031c177728ac9d04b2487e4291d8281691ed5244033114bf5`; deploy script timed out during a cluster-wide kubelet pod-sandbox incident, then Kubernetes rollout completed after pending pod cleanup and node recovery.
- Live operation-event image -> `localhost:5000/heureka-service:task-010-operation-events-20260701b` and `localhost:5000/heureka-api-gateway:task-010-operation-events-20260701b`; new pods `heureka-service-7f5d8889bd-zlvgz` and `heureka-api-gateway-557b49f455-zbxwg` reached `1/1 Running` with `0` restarts.
- Post-operation-event public smoke -> `/health`, `/api/health`, and `/dashboard/operations` returned `200`; `/api/heureka/dashboard/operations/history` without token returned expected `401`.
- Post-operation-event Prisma live smoke -> deployed service pod has `heurekaOperationEvent` model and can query the table; initial count was `0` before validation event insertion.
- Post-operation-event durable validation event -> inserted idempotent non-business `validation_operation_event` with idempotency key `task-010-operation-events-20260701b-live-smoke`; attempted update was blocked by the append-only trigger and saved status remained `success`.
- Post-operation-event authenticated gateway smoke -> `GET /api/heureka/dashboard/operations/history` returned `200`, `success=true`, `eventCount=35`, `durableCount=1`, `operationLogTotal=1`, safety `{readOnly:true, returnsRawPayload:false}`, first durable event `validation_operation_event`, and only `[MISSING: ecosystem-wide shared operation/audit schema package]` remained.
- Operations derived timeline self-test -> PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options '{"types":["node"]}' services/heureka-service/src/heureka/dashboard/dashboard-operations-history.self-test.ts`.
- Operations timeline build validation -> PASS with `npm --prefix shared run build`; PASS with `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`.
- Operations timeline deploy `./scripts/deploy.sh task-010-ops-timeline-20260701` -> PASS; active image `localhost:5000/heureka-service:task-010-ops-timeline-20260701`; pod `heureka-service-6c7fb48897-cnpxs` `1/1 Running`.
- Post-operations-timeline public smoke -> `/health` returned `200`, `/dashboard/operations` returned `200`; protected `/heureka/dashboard/operations/history` without token returned expected `401`.
- Post-operations-timeline authenticated in-pod smoke -> `GET /heureka/dashboard/operations/history` returned `200`, `success=true`, `operationEvents=34`, `feeds=22`, `orders=4`, `products=7`, `settings=1`, and durable-audit `[MISSING: ...]` markers remained explicit.
- Post-batch Catalog source-evidence scan for 24 blocked products -> `blockedCount=24`; the 11 `MISSING_PRIMARY_IMAGE` rows had Catalog media records without URLs, `sourceDataKeys=[]`, `externalRefsKeys=[]`, and `imageUrls=0`; apparent stock hints were generic description text, not stock evidence.
- Allegro guarded API source-evidence scan for the 11 `MISSING_PRIMARY_IMAGE` offer IDs -> `scannedOffers=47`, `targetCount=11`, `foundCount=11`, `withImagesCount=0`, `orderHistoryPartialCount=11`; every target had `syncSource=ORDER_HISTORY`, `syncStatus=PARTIAL`, `recoveryStatus=PARTIAL`, `missingFields` including `images`, `stockQuantity=0`, and raw data limited to `fetchError`/`orderEvidence`/`orderOfferHarvest`.
- TASK-010 parallel shared-schema audit -> no reusable cross-service operation/audit schema package exists in `shared` or `logging-microservice`; the only reusable runtime logging contract is generic `POST /api/logs`, so Heureka implemented a non-emitting local adapter and preserved `[MISSING: ecosystem-wide shared operation/audit schema package]` for platform adoption.
- TASK-010 parallel stock audit -> Catalog active-products plus Warehouse availability still identify 24 zero-stock candidates; 23 are Allegro order-history-only/partial rows with `stockQuantity=0`, `PROD9998` has no Allegro stock source, and pre-fix live Heureka readiness returned `PRODUCT_NOT_FOUND=24` only because protected Catalog reads lacked internal service headers.
- TASK-010 parallel media audit -> 11 affected products have Catalog media count `0`, image-like URLs `0`, Allegro `ORDER_HISTORY`/`PARTIAL`, raw images `0`, and no approved image files/URLs available for safe backfill.
- TASK-010 Catalog-auth/schema validation -> PASS: `shared/clients/catalog-client.service.self-test.ts`; PASS: `operation-event.service.self-test.ts`; PASS: `dashboard-operations-history.self-test.ts`; PASS: `feed-readiness-settings.self-test.ts`; PASS: `npm --prefix shared run build`; PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; PASS: `npm --prefix services/api-gateway run build`; PASS: `git diff --check`.
- TASK-010 Catalog-auth/schema IPS gates -> PASS: `npm run ips:audit`; PASS: `npm run ips:pre-coding`; PASS: `npm run ips:deployment-readiness`.
- Catalog-auth/schema deploy `./scripts/deploy.sh task-010-catalog-auth-schema-20260701` -> images pushed with service digest `sha256:9f2e36eb2e3bbc6212cb71c465cf63fd52bc79fba8cc9e15721ad8488e02cccc` and gateway digest `sha256:90f2d7875308e01765e73e84ae3e561f6b501870fe7a07a2f4ed0a24912b4d4f`; script timed out during slow node image pulls, then manual rollout status completed successfully for both deployments.
- Live Catalog-auth/schema image -> `localhost:5000/heureka-service:task-010-catalog-auth-schema-20260701` and `localhost:5000/heureka-api-gateway:task-010-catalog-auth-schema-20260701`; new pods `heureka-service-7c86c479b5-7p67b` and `heureka-api-gateway-747897bdb6-qmr96` reached `1/1 Running` with `0` restarts.
- Post-Catalog-auth/schema public smoke -> `/health` returned `200` with `service=heureka-service`; `/api/health` returned `200` with `service=heureka-api-gateway`; `/dashboard/operations` returned `200`; unauthenticated `/api/heureka/dashboard/operations/history` returned expected `401`.
- Post-Catalog-auth/schema authenticated gateway smoke -> `GET /api/heureka/dashboard/operations/history?feedType=heureka_cz` returned `200`, `success=true`, `eventCount=35`, `schemaRef=heureka.operation.audit.v1`, durable event `validation_operation_event`, audit envelope schema `heureka.operation.audit.v1`, logging service `heureka-service`, and `returnsRawPayload=false`.
- Post-Catalog-auth/schema readiness smoke for the 24 affected product IDs -> `POST /heureka/feed/readiness/bulk` returned `201`, `success=true`, `productCount=24`, blocker counts `ZERO_STOCK=24` and `MISSING_PRIMARY_IMAGE=11`; `PRODUCT_NOT_FOUND` is absent after authenticated Catalog product reads.

- `[RESOLVED: bulk selected-product workflow preview in dashboard UI]`; dashboard selection now calls existing read-only `POST /heureka/feed/readiness/bulk` and summarizes selected-product readiness blockers without mutating feed, stock, media, or external Heureka state.


## Goal Completion Audit

Current audit date: 2026-07-01. Completion is not claimed until every item below is proven by current source, validation, and live evidence.

| Requirement from objective | Current evidence | Status |
| --- | --- | --- |
| Compare Heureka against existing sales-channel frontend, landing, dashboard, Catalog, Orders, logging, and microservice patterns | Feature Matrix and Ready Implementation Tasks map Heureka against Allegro, Bazos, Aukro, and FlipFlop with file/route evidence. | Proven for code parity baseline; keep active for new drift. |
| Save an Intent Preservation aligned plan before implementation | This checklist preserves Vision -> Goal Impact -> System -> Feature -> Task -> Execution Plan -> Coding Prompt -> Code -> Validation and extends `TASK-010-channel-parity-gap-plan.md`. | Proven. |
| Implement public landing, hosted Auth, callback handling, and protected dashboard shell parity | Current live smoke on `task-010-parity-hardening-20260701b` returned `200` for `/`, `/login`, `/register`, `/auth/callback`, `/dashboard`, `/dashboard/feed`, `/dashboard/orders`, `/dashboard/operations`, `/dashboard/settings`, `/dashboard/admin/users`; protected API routes returned expected unauthenticated `401`. | Proven. |
| Implement dashboard product workflow, feed status/history/settings, orders, operations, and admin surfaces | Dashboard services/controllers/public shell routes are implemented; self-tests and live smoke recorded in Validation Bundle; authenticated operations history smoke returned schema-aware durable events. | Proven for current parity surface. |
| Implement Catalog/Warehouse integration following shared service contracts | Catalog protected reads/writes use internal service headers; Warehouse batch availability is used for list readiness; `catalog-client.service.self-test.ts` and readiness smoke pass. | Proven for code contract; product data still blocks 24 active items. |
| Implement Orders communication | Heureka order ingestion contract matches `orders.create.v1`; `npm run verify:heureka-order-ingestion` passed in TASK-010 validation. | Proven for code contract; runtime token source remains `[UNKNOWN]`. |
| Implement logging/operation evidence parity | `heureka_operation_events`, writer hooks, read model, `heureka.operation.audit.v1`, and dashboard operations timeline are implemented and deployed. | Proven locally; ecosystem-wide shared operation/audit schema package remains `[MISSING]`. |
| Implement deploy/runtime microservice parity | Docker, k8s config, API gateway, ingress split, deploy script, live service/gateway deployments, gateway orders routing, and health/dependency checks are implemented. | Proven by build/deploy/live smoke and dependency-health/gateway-route self-tests. |
| Complete external Heureka shop/import readiness | `npm run verify:heureka-external-readiness` now records the public XML feed contract and keeps owner-only Heureka approval/import evidence as explicit blockers; product feed still excludes zero-stock/image-blocked products. | Not complete: `[UNKNOWN: shop approval]`, `[UNKNOWN: current external Heureka import/feed-validity result]`, `[MISSING: owner-supplied e-shop registration legal/company fields]`, `[MISSING: Heureka merchant/API key approval evidence]`. |
| Make all current active products feed-ready if they should be sellable | Latest in-pod `npm run verify:heureka-blocked-product-lanes` confirmed active `41`, ready `16`, blocked `25`; blockers remain `ZERO_STOCK=25`, `MISSING_PRIMARY_IMAGE=12`, plus one Catalog/pricing content blocker for `8edc51f2-bed2-433f-8a3c-5738b49a02e1`; current behavior correctly excludes blocked products from public feed. | Not complete: `[MISSING: authoritative stock source]`, `[MISSING: approved image URLs/files]`, `[MISSING: public category/price source for one product]`, `[UNKNOWN: which zero-stock products should be listed]`. |
| Use sub-agents where safe | Parallel read-only agents audited shared schema, stock, media; continuation parity, external-onboarding, and product-data explorers completed and were integrated into this checklist. | Proven for this continuation pass. |

Fresh continuation evidence captured on 2026-07-01:

- Current deployments: `heureka-service` and `heureka-api-gateway` both run `task-010-parity-hardening-20260701b`, `available=1`, `updated=1`.
- Public route smoke: `/`, `/login`, `/register`, `/auth/callback`, `/dashboard`, `/dashboard/feed`, `/dashboard/orders`, `/dashboard/operations`, `/dashboard/settings`, `/dashboard/admin/users`, `/health`, and `/api/health` returned `200`.
- Protected route boundary smoke: `/api/heureka/dashboard/summary`, `/api/heureka/dashboard/orders`, `/api/heureka/dashboard/feed/status`, `/api/heureka/dashboard/operations/history`, and `/heureka/dashboard/me` returned expected `401` without token.
- Targeted self-tests: `shared/clients/catalog-client.service.self-test.ts` -> PASS; `services/heureka-service/src/heureka/operations/operation-event.service.self-test.ts` -> PASS.
- Fresh parity code-gap audit found four implementable gaps: unguarded public order reads, unguarded public feed regeneration, dropped dashboard product filters, and raw gateway payload logging/status flattening; live post-fix smoke then exposed and resolved dashboard-list category override drift.
- Fresh external-onboarding audit found no current owner-supplied legal/company fields, shop approval evidence, or external import result evidence in the repo or public routes; the missing verifier contract is now covered by `npm run verify:heureka-external-readiness`.
- TASK-010 parity hardening validation -> PASS: `dashboard-list-products.self-test.ts`; PASS: `gateway-route-parity.self-test.ts`; PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; PASS: `npm --prefix services/api-gateway run build`; PASS: `git diff --check`.
- Latest product-data audit -> active Catalog products `41`, ready `16`, blocked `25`; live blockers remain `ZERO_STOCK=25`, `MISSING_PRIMARY_IMAGE=12`, `MISSING_CATEGORY=1`, and `PRICE_MISSING=1`; `PRODUCT_NOT_FOUND` and `MISSING_DESCRIPTION` are `0`.
- Fresh stock evidence -> Warehouse has `24/24` rows for zero-stock products, all `totalAvailable=0`; Allegro has `23` matched persisted offer rows, all `ORDER_HISTORY/PARTIAL`, positive persisted stock `0`, current-authoritative stock rows `0`, stock snapshot rows `0`; product `a2e15cc0-1a94-4faf-a82f-64afea9e9817` has no Allegro offer row.
- Fresh image evidence -> 11 missing-image products have Catalog media rows `0`, public media URLs `0`, Heureka snapshot `IMGURL=0`, image-like public source URLs `0`, Heureka profile image evidence `0`, Allegro profile image values empty `11/11`, Allegro direct/raw image URL count `0`.
- Parity-hardening deploy `./scripts/deploy.sh task-010-parity-hardening-20260701` -> PASS; images pushed with service digest `sha256:262b12cdaa9b045e28b9b0c839b6f00e393e4ccf6b22dfa4c2483fcc73988e60` and gateway digest `sha256:7b51d4b451e376936375e3267f7baa35710464b7a3695b9c517d9925960804f1`; live smoke confirmed public health/dashboard `200`, unauthenticated order reads `401`, unauthenticated feed regeneration `401`, and dashboard product filters reach the service.
- Dashboard marketplace-category consistency fix -> PASS: `dashboard-list-products.self-test.ts`; PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; PASS: `gateway-route-parity.self-test.ts`; PASS: `npm --prefix services/api-gateway run build`; PASS: `npm run ips:audit`; PASS: `npm run ips:pre-coding`; PASS: `npm run ips:deployment-readiness`; PASS: `git diff --check`.
- Parity-hardening/category deploy `./scripts/deploy.sh task-010-parity-hardening-20260701b` -> service image digest `sha256:4f6d25f62712190c57e9545b171f9ff9b0d51dcb7c43255a286e23e6fad9ee7c`; gateway image digest `sha256:7b51d4b451e376936375e3267f7baa35710464b7a3695b9c517d9925960804f1`; deploy script timed out during cluster pod-sandbox/image-pull pressure, failed sandbox pods were deleted, and manual rollout completed successfully.
- Live parity-hardening/category image -> `localhost:5000/heureka-service:task-010-parity-hardening-20260701b` and `localhost:5000/heureka-api-gateway:task-010-parity-hardening-20260701b`; pods `heureka-service-5699777db7-9ssdh` and `heureka-api-gateway-56d68cd984-8j667` reached `1/1 Running`, `0` restarts.
- Post-parity-hardening/category public smoke -> `/health`, `/api/health`, and `/dashboard` returned `200`; unauthenticated `/heureka/orders`, `/heureka/orders/test-order`, `/heureka/feed/regenerate`, and `/api/heureka/feed/regenerate` returned `401`.
- Post-parity-hardening/category authenticated dashboard list smoke -> `GET /heureka/dashboard/catalog-products?limit=10&workflowStatus=blocked&gap=stock` returned `200`, `returned=10`, filters include `workflowStatus=blocked` and `gap=stock`; first samples now show Catalog Heureka marketplace category text and no stale `category` gap where readiness category is satisfied.
- Blocked-product lane verifier implementation target -> `scripts/verify_heureka_blocked_product_lanes.js` plus `npm run verify:heureka-blocked-product-lanes`; the script must read active Catalog products, Heureka bulk readiness, Warehouse batch availability, and Catalog media/feed snapshots; it must emit machine-actionable stock/media/catalog-content lanes and must not mutate Catalog, Warehouse, feed, stock, media, or external Heureka state.
- Blocked-product lane verifier validation -> first live run exposed the Catalog list contract maximum `limit=100`; verifier default was corrected from `200` to `100` and capped at `100`, then `node --check scripts/verify_heureka_blocked_product_lanes.js`, `npm run ips:audit`, `npm run ips:pre-coding`, `npm run ips:deployment-readiness`, and `git diff --check` passed.
- Blocked-product lane verifier deploy -> first `./scripts/deploy.sh task-010-blocked-lane-verifier-20260701` completed but verifier failed on the Catalog `limit` contract; corrected deploy `./scripts/deploy.sh task-010-blocked-lane-verifier-20260701b` built/pushed service digest `sha256:c5783f5a8dbd148bf117d6bdca1e3002b11cbf7c36063ed837248e13ea1213ae` and gateway digest `sha256:308402075751f9a54b97348a39243bcbd6f0ee55fc41416c1f9fbafd206aabfa`.
- Blocked-product lane verifier rollout -> deploy script timed out during Kubernetes pod sandbox/termination backlog, only `ContainerCreating` replacement pods were deleted, rollout then completed; live deployments now run `localhost:5000/heureka-service:task-010-blocked-lane-verifier-20260701b` and `localhost:5000/heureka-api-gateway:task-010-blocked-lane-verifier-20260701b`, both `available=1`, `updated=1`.
- Post-verifier-deploy smoke -> `/health`, `/api/health`, and `/dashboard` returned `200`.
- Latest blocked-lane verifier smoke -> in-pod `npm run verify:heureka-blocked-product-lanes` returned `contractVersion=heureka-blocked-product-lanes.v1`, `readOnly=true`, `mutations=[]`, active Catalog products `41`, readiness `ready=16`, `blocked=25`, `unknown=0`; stock lane is blocked with `25` zero-stock products and Warehouse rows for all `41` active products; media lane is blocked with `12` missing primary images; Catalog content lane is blocked for `8edc51f2-bed2-433f-8a3c-5738b49a02e1` (`EAN4893575894` / `Cvetki gorshki`) with missing public category text and public VAT-inclusive price.
- Readiness-lanes dashboard validation -> PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options '{"types":["node"]}' services/heureka-service/src/heureka/dashboard/dashboard-list-products.self-test.ts`; PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; PASS: `npm run ips:audit`; PASS: `npm run ips:pre-coding`; PASS: `npm run ips:deployment-readiness`; PASS: `git diff --check`.
- Readiness-lanes dashboard deploy `./scripts/deploy.sh task-010-readiness-lanes-dashboard-20260701` -> PASS; service digest `sha256:819e0629f2d39c4e0d01fd34d1ddb98b53c81c9e6ddc71bcf8d925758bc52fb3`; gateway digest `sha256:308402075751f9a54b97348a39243bcbd6f0ee55fc41416c1f9fbafd206aabfa`; live deployments `available=1`, `updated=1`.
- Post-readiness-lanes public smoke -> `/health`, `/api/health`, `/dashboard`, and `/dashboard/operations` returned `200`; unauthenticated `/heureka/dashboard/readiness/lanes` returned expected `401`.
- Post-readiness-lanes authenticated in-pod smoke previously proved dashboard lane shape; latest blocker verifier supersedes counts with active Catalog products `41`, readiness `ready=16`, `blocked=25`, stock lane `25`, media lane `12`, and Catalog content lane `1`.
- External-onboarding verifier implementation target -> `scripts/verify_heureka_external_readiness.js` plus `npm run verify:heureka-external-readiness`; the script must check the public Heureka XML feed, optionally consume a protected readiness-lanes JSON URL when a bearer token is supplied, emit `heureka-external-readiness.v1`, remain read-only, and keep owner-only external Heureka approval/import facts as `[MISSING]` / `[UNKNOWN]` blockers instead of inventing or submitting them.
- External-onboarding verifier evidence -> read-only public feed preview endpoint is reachable at `/heureka/feed/preview?type=heureka_cz` and returns `HTTP 200`, XML content type, `x-heureka-feed-status: valid`, `x-heureka-feed-read-only: true`, and non-empty `SHOPITEM` entries; repo has no Heureka merchant credential secret reference and no legal/company/IČO/shop-approval model fields. Remaining external blockers stay `[UNKNOWN: shop approval]`, `[UNKNOWN: current external Heureka import/feed-validity result]`, `[MISSING: owner-supplied e-shop registration legal/company fields]`, `[MISSING: Heureka merchant/API key approval evidence]`.
- Dependency-health parity validation -> PASS: `npm run verify:health-dependencies`; PASS: `npm --prefix shared run build`; PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npm --prefix services/heureka-service run build`; PASS: `npm --prefix services/api-gateway run build`; `/health` remains a lightweight probe while `/health/dependencies` returns `heureka.dependency-health.v1`.
- Gateway orders route parity validation -> PASS: `LOGGING_SERVICE_URL=http://logging-microservice:3367 npx ts-node --skip-ignore --compiler-options '{"types":["node"]}' services/api-gateway/src/gateway/gateway-route-parity.self-test.ts`; `/api/heureka/orders*` is now classified as Heureka-owned and legacy Aukro fallback remains for unknown paths.
- README parity refresh -> documented service port `3800`, API gateway port `3801`, dashboard routes, `/api/heureka/orders*` routing, dependency health, and current verifier commands.
- TASK-010 source-parity verifier -> `NPM_CONFIG_CACHE=/tmp/heureka-npm-cache npm run verify:task-010-source-parity` now runs the non-mutating source parity bundle: dependency-health self-test, feed preview read-only self-test, public dashboard route self-test, gateway route/redaction/optional-route parity self-test, shared build, Heureka service build, API gateway build, and order-ingestion contract verifier. The explicit cache path avoids the full Docker data npm cache partition. Blocked-product data lanes and owner-controlled external Heureka approval checks remain separate because they depend on live owner/data evidence.
- Route cleanup deploy -> `./scripts/deploy.sh task-010-route-cleanup-20260701` initially timed out because new pods hit Kubernetes/containerd sandbox creation errors; deleting only the stuck `0/1 ContainerCreating` rollout pods let ReplicaSets recreate them, then `kubectl rollout status` completed for both deployments. Live deployments run `localhost:5000/heureka-service:task-010-route-cleanup-20260701` and `localhost:5000/heureka-api-gateway:task-010-route-cleanup-20260701`, both `available=1`, `updated=1`. Final smoke: `/dashboard/orders`, `/dashboard/operations`, `/dashboard/settings`, `/health`, and `/api/health` returned `200`; `/health/dependencies` returned `heureka.dependency-health.v1` with `status=ok`; `/heureka/feed/preview?type=heureka_cz` returned `HTTP 200`, `x-heureka-feed-read-only: true`, `x-heureka-feed-status: valid`, and `7` `SHOPITEM`; unauthenticated `/api/heureka/orders`, `/api/settings`, and `/api/import/test` returned expected `401` at the auth boundary while optional service fail-closed `501 SERVICE_NOT_CONFIGURED` is covered by gateway self-test without printing or manufacturing tokens.
- Stock authority explorer evidence -> Warehouse batch availability returned `25/25` affected rows with `totalQuantity=0`, `totalReserved=0`, `totalAvailable=0`; Catalog Heureka marketplace profiles were readable for `25/25` and had no stock-like values; 23 Allegro local offers are non-authoritative `ORDER_HISTORY_ONLY` / `ORDER_HISTORY` / `PARTIAL` rows with `stockQuantity=0`, `quantity=0`, and `rawStockAvailable=null`; current Allegro stock snapshot rows for these products/offers are `0`; the owner-approved 2026-06-29 Allegro complete physical stock recheck found only `9` unique current-stock-authoritative offers and Warehouse already matched them, so there is no hidden Allegro source for the `25` Heureka zero-stock products; `PROD9998` and `EAN4893575894` have no Allegro offer/snapshot authority.
- Media backfill explorer evidence -> no approved public image URLs/files were found for the 12 `MISSING_PRIMARY_IMAGE` products in Catalog media, Catalog Heureka snapshots, current public feed preview, docs, or reference channel status data; every affected product also remains `ZERO_STOCK`; `EAN4893575894` / `Cvetki gorshki` additionally lacks public Heureka category text and VAT-inclusive price.
- Owner handoff artifact -> `docs/orchestrator/TASK-010-data-owner-handoff.md` records the exact stock/media/external owner lanes, product IDs, allowed systems, forbidden actions, required owner decisions, and post-data-change validation commands.


## Remaining Parallel Data Remediation Lanes

| Lane | Status | Owner Role | Objective | Allowed Systems | Forbidden Actions | Dependency | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Stock authority lane | blocked | Warehouse/Allegro stock owner | Decide and populate authoritative stock for 25 zero-stock products if they should be sellable. | Warehouse read reports, Allegro current-stock dry-run only with owner approval/seller proof, Warehouse `POST /api/stock/set` only after approved source evidence. | Do not infer stock from order history; do not treat token refresh alone as stock authority; do not write Warehouse without product IDs, warehouse ID, quantity, actor, reason code, and reference. | `[MISSING: authoritative current stock source for 25 Heureka zero-stock products]`; owner-approved Allegro recheck found no hidden source; persisted Allegro order-history rows are `PARTIAL` and not current stock authority; `[MISSING: owner approval for Warehouse mutation]` | `npm run verify:heureka-blocked-product-lanes` stock lane, Warehouse verifier, plus Heureka readiness for affected product IDs. |
| Media backfill lane | blocked | Catalog/media owner | Add primary public images for 12 products only if they are intended to become sellable. | Catalog `GET /api/media/product/:id`, guarded `POST /api/media` or `POST /api/media/upload`, approved external image files/URLs. | Do not invent product images; do not add non-public or unapproved image URLs. | Guarded Catalog/reference evidence found no approved image URLs/files; `[MISSING: approved image URLs/files]`; stock lane can run independently but all 12 image products are currently zero-stock. | `npm run verify:heureka-blocked-product-lanes` media lane, Catalog media read, Catalog Heureka snapshot `IMGURL`, Heureka readiness blocker removal for `MISSING_PRIMARY_IMAGE`. |
| External Heureka onboarding lane | blocked | Channel owner | Prove shop approval/import readiness and submit missing business/legal facts through owner-controlled external Heureka flow. | `npm run verify:heureka-external-readiness`, public feed header check, owner/browser-operated Heureka UI read-only status, owner-supplied legal/company fields. | Do not submit external forms or mutate external Heureka without owner approval; do not expose secrets. | `[UNKNOWN: shop approval]`; `[UNKNOWN: current external Heureka import/feed-validity result]`; `[MISSING: owner-supplied e-shop registration legal/company fields]`; `[MISSING: Heureka merchant/API key approval evidence]` | `heureka-external-readiness.v1`, current external UI/feed-validity evidence, public XML check, and owner approval record. |

Deploy gate, only after successful build/tests:

```bash
./scripts/deploy.sh
curl -I https://heureka.alfares.cz/
curl -I https://heureka.alfares.cz/health
curl -I https://heureka.alfares.cz/dashboard
curl -I https://heureka.alfares.cz/heureka/dashboard/me
curl -I https://heureka.alfares.cz/dashboard/orders
curl -I https://heureka.alfares.cz/dashboard/feed
curl -I https://heureka.alfares.cz/dashboard/operations
curl -I https://heureka.alfares.cz/dashboard/settings
curl -I https://heureka.alfares.cz/heureka/dashboard/orders
curl -I https://heureka.alfares.cz/heureka/dashboard/feed/status
curl -I https://heureka.alfares.cz/heureka/dashboard/operations/history
```
